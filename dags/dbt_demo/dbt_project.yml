name: dbt_demo
version: '1.0.0'
config-version: 2

profile: dbt_demo

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_demo:
    silver:
      +materialized: table
      +schema: silver
      +tags: ["silver"]
    gold:
      +materialized: table
      +schema: gold
      +tags: ["gold"]
    spark_silver:
      +materialized: table
      +tags: ["spark"]
    spark_gold:
      +materialized: table
      +tags: ["spark"]
